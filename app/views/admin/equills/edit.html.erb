<% provide(:title, "Editando eQuill #{@equill.name}") %>

<header class="content-header">
  <div class="container-fluid">
    <div class="row mb-2">
      <div class="col-sm-6">
        <h1>eQuill: <%= @equill.name %>
        <br>
        <small>Editando</small>
        </h1>
      </div>
      <div class="col-sm-6">
        <ol class="breadcrumb float-sm-right mt-2">
          <li class="breadcrumb-item">
            <%= link_to "eQuills", admin_equills_path %>
          </li>
          <li class="breadcrumb-item active">Editando eQuill</li>
        </ol>
      </div>
    </div>
  </div><!-- /.container-fluid -->
</header>

<%= render 'form', equill: @equill %>